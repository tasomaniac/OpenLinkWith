language: android

android:
  components:
    - tools
    - tools
    - platform-tools
    - build-tools-24.0.2
    - android-24
    - extra-google-m2repository
    - extra-android-m2repository

jdk:
  - oraclejdk8

script: "./gradlew clean check"

branches:
  except:
    - gh-pages

sudo: false

cache:
  directories:
    - $HOME/.gradle
